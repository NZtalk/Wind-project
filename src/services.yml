apiVersion : v1
kind: Service
metadata:
    name: itw-project-service
spec:
    selector:
        app: itw-project
    ports:
        - protocol: TCP
          port: 8000
          targetPort: 8000
    type: LoadBalancer